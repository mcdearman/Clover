[package]
name = "generate"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
quickcheck = { version = "=1.0.3", default-features = false }

[build-dependencies]
serde_json = "1"
